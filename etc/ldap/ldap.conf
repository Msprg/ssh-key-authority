#
# LDAP Defaults
#

# System-wide CA bundle for LDAPS / StartTLS certificate verification.
# Without this, libldap (used by PHP's ldap extension) has no trust
# anchors and every ldaps:// connection fails with
# "Can't contact LDAP server".
TLS_CACERT /etc/ssl/certs/ca-certificates.crt
